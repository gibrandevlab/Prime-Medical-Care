// Deprecated: authentication now uses Pegawai via backend API returning
// { token, role, pegawaiId, user: { nama, email } }
// This file is intentionally left as a placeholder and should not be used.

/*
enum UserRole { admin, petugas }

class User {
  int? id;
  String? username;
  String? password;
  UserRole? role;
  int? pegawaiId;

  User({this.id, this.username, this.password, this.role, this.pegawaiId});
}
*/
